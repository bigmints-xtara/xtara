
============================================================
[2026-06-09T10:40:22.374Z] delegate_to_cli → pi
CWD: /home/bigmints/projects/xtara/xtara-web
============================================================


## DELIVERY COMPLETE

Summary of what was built:

1. **`src/types/admin.ts`** — Extended the `Spark` interface with `draft: boolean` and `inReview: boolean` fields, matching the 3-state status pattern used by `Story` and other admin entities.

2. **`src/lib/admin/configs/sparksConfig.ts`** — New admin config for Sparks, modeled exactly after `storiesConfig.ts`:
   - `entityName: 'Spark'`, `entityNamePlural: 'Sparks'`, `collectionName: 'sparks'`
   - `createEmpty()` initializes with `draft: true`, `inReview: false`, `published: false`
   - `getDomain()` uses `type` for domain grouping/filtering
   - `getSubtitle()` returns `type`
   - `getStatus()` implements the same 3-state logic (published → inReview → draft)
   - `getSearchText()` searches both title and type
   - `toFirestore()` / `fromFirestore()` handle serialization

3. **`src/components/admin/editors/SparkEditor.tsx`** — New editor component with fields for Title, Type, and Status (dropdown controlling the 3 boolean fields identically to `StoryEditor`).

4. **`src/app/admin/sparks/page.tsx`** — Replaced the "Coming Soon" placeholder with the `AdminMasterDetail<Spark>` component, wired to `sparksConfig` and `SparkEditor`, matching the exact pattern used by the Stories admin page.

[2026-06-09T10:43:50.297Z] delegate_to_cli resolved
