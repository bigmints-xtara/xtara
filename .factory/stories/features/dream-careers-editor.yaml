feature:
  name: Create CareerPathEditor component
  slug: dream-careers-editor
description: Create src/components/admin/editors/CareerPathEditor.tsx.
  Read-mostly editor with read-only overview section (userId, careerName,
  matchScore, expectedSalaryRange), editable fields (title, description,
  whatYouDo, whyItMatters, matchReasoning), read-only badge chips for
  strengths/archetypes/streamSuggestions, read-only lists for careerPathway and
  relatedCareers. Save button only sends editable fields.
target:
  app: main
phase: 2
dependsOn:
  - dream-careers-config
status: draft
