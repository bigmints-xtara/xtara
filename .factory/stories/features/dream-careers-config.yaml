feature:
  name: Create dreamCareersConfig
  slug: dream-careers-config
description: "Create src/lib/admin/configs/dreamCareersConfig.ts with
  AdminConfig<AdminCareerPath>. Follow sparksConfig.ts pattern exactly.
  Collection: 'career_paths'. getStatus always returns 'published'.
  availableStatuses: ['all']. getDomain uses archetypes[0]. getAvailableDomains
  collects unique archetypes[0] values."
target:
  app: main
phase: 1
dependsOn: []
status: draft
